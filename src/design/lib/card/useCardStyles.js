import { classNames } from '../utils';

export function useCardStyles() {
  const classes = [];
  return classNames(classes);
}

export function useCardHeaderStyles() {
  const classes = [];
  return classNames(classes);
}
export function useCardBodyStyles() {
  const classes = [];
  return classNames(classes);
}
export function useCardFooterStyles() {
  const classes = [];
  return classNames(classes);
}
